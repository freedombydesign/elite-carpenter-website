{
  "name": "elite-carpenter-website",
  "version": "1.0.0",
  "description": "Elite Carpenter Home Renovation Website",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build"
  },
  "dependencies": {
    "twilio": "^5.3.1"
  },
  "note": "Twilio only needed if using WhatsApp notifications (/api/whatsapp-notify). Default is FREE Telegram (/api/telegram-notify) which has no dependencies."
}
